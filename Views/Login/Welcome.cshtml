@model SignalR_chat.Models.UserModel
@{
    ViewData["Title"] = "Welcome";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <h2 class="mt-5">Welcome, @Model.userName!</h2>
            <p class="mt-3">You have successfully logged in.</p>
            <a href="/Account/Logout" class="btn btn-primary mt-3">Logout</a>
        </div>
    </div>
</div>
